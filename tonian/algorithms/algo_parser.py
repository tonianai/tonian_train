
from typing import Dict, Union


class AlogParser:
    
    def __init__(self, config: Union[str, Dict]) -> None:
        """Parses the config and creates an algorithm based on the config file
        Args:
            config (Union[str, Dict]): [description]
        """
        
        if isinstance(config, str):
            
        elif isinstance(config, Dict):
            
        else:
            throw E
        
        
        
        
        pass